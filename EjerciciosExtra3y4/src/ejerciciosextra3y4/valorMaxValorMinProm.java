/*
 * Realice un programa que calcule y visualice el valor máximo, 
 * el valor mínimo y el promedio de n números (n>0). 
 * El valor de n se solicitará al principio del programa 
 * y los números serán introducidos por el usuario. 
 * Realice dos versiones del programa, una usando el bucle “while” 
 * y otra con el bucle “do - while”.
 */
package ejerciciosextra3y4;

import java.util.Scanner;

/**
 *
 * @author santi
 */
public class valorMaxValorMinProm {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
     Scanner leer = new Scanner(System.in);
       int numMax =0;
       int numMin = 0;
       int num = 0;
    }
}
       /*do {
           System.out.println("Ingrese la cantidad de numeros que desea ingresar");
           num = leer.nextInt();
           if (num>numMax){
           numMax = num;
           } else{
             // int numMin = num;
           }*/
           
     /*  } //while ( num > 0 && num < );
        //System.out.println("Ingrese la cantidad de numeros que desea ingresar");
        //while (true)
        // 
{}
*/
    

